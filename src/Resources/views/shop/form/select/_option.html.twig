{# @var choice \Symfony\Component\Form\ChoiceList\View\ChoiceView #}
<div class="ssrn-option" data-value="{{ choice.value }}">
    <span class="ssrn-option-label">{{ choice.label|trans(choice.labelTranslationParameters) }}</span>
    <span class="ssrn-option-remind-me">Remind me</span>
</div>
